# 从 V0.1.2 升级到 V0.2.0

推荐直接用 V0.2.0 完整工程覆盖 GitHub 仓库后重新运行 `Build Windows EXE`。

用户电脑原来的 AppData 配置不需要删除。V0.2.0 对旧 config.json 做了向后兼容读取，新增字段会使用默认值。

升级后建议：
1. 打开“打工人搭子”页配置重要联系人、工作目录、微信文件目录。
2. 点击“建立/更新文件索引”。
3. 保持“开发者兼容模式：暴露全部低层工具”关闭。
4. 把新的 `xiaozhi-agent-prompt.txt` 替换到小智智能体提示词。
5. 重新连接 MCP。
6. 先用文件传输助手或测试微信号验证微信发送，再测试真实联系人。
