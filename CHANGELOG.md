# Changelog

## 0.3.0
- 新增多来源联网调研：搜索结果提取、临时标签页逐页读取、来源去重与本地摘要。
- 新增联网调研 Word 报告，可选只调用一次用户 API 做最终综合；API 不可用时自动退回纯本地提取式报告。
- 新增联网调研 → 微信准备发送链路，继续保留发送确认。
- 新增 Excel `split_by_column`：按列不同值拆多个文件或多个 Sheet，并生成拆分清单。
- 新增 Excel 条件修改、值替换、空值填充、去重、安全派生列计算。
- 新增 Excel 两表键值匹配合并（VLOOKUP/XLOOKUP 类场景）。
- 新增整目录 Excel 合并。
- 新增文件夹批量处理：同一自然语言计划只规划一次并复用，避免每个文件重复调用 API。
- Excel 自然语言白名单新增 split/deduplicate/fill/replace/calculate/modify_where。
- `workmate` 新增联网调研路由与批量 Excel 路由。
- 新增 7 个自动化测试；当前测试 19/19 通过。

## 0.2.0
- 新增 `workmate` 本地优先统一入口。
- API 从“必需”改为“可选语义能力”。
- 新增任务中心、SQLite 持久化任务和定时发送。
- 新增重要联系人/老板雷达（实验性）。
- 新增微信联系人读取、近期附件发现、发送后 UIA 验证。
- 新增本地文件内容索引和搜索。
- 新增本地网页/PDF 提取式摘要。
- 新增 Excel 受限操作计划：row_total/group_sum/filter/mark_below_target/sort。
- 新增 Excel 多文件合并、分析工作表、本地分析。
- 新增 PPTX 本地生成/读取、Excel→PPT 本地汇报。
- 新增网页 HTML table → XLSX。
- 新增“照旧/跟上次一样”工作上下文。
- 新增截图 + 微信本地链路。
- MCP 默认 compact 模式，只暴露高层工具，低层工具改为开发者可选。
- API 模型名称不再硬编码默认值。
- 定时发送加入明确授权、持久化、失败重试和任务状态。

## 0.1.2
- Excel 逐行合计写回。
- 演示模板分析写回。

## 0.1.1
- API 测试改后台线程，修复 GUI 假死。
