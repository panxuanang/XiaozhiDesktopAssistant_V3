# V0.4.0 性能重构版

目标：**功能不删，简单操作尽量恢复早期版本“说完就动”的速度，同时保留 V0.3 的 Excel、Word/PDF/PPT、网页调研、微信、任务中心和定时交付。**

## 1. MCP 普通模式只暴露 4 个高层工具

- `workmate`：电脑任务统一入口
- `task_center`
- `search_work_files`
- `performance_report`

`desktop_agent` 和几十个低层工具只在“开发者兼容模式”暴露。这样小智后台不需要在几十个相似工具中选，也不会每轮携带巨大的工具 Schema。

## 2. 零 API 快车道

以下明确任务先走本地确定性路由，不调用用户自己的 AI API：

- 打开常见网站、URL、浏览器
- 百度/必应/Google 搜索
- 打开常见程序和明确文件
- 最小化/最大化/恢复窗口
- 设置音量、截图
- 明确“在某窗口点击某控件”的 UIA 操作
- 微信准备发送文本/文件
- 明确文件移动/复制

本地规则未命中才进入原有 `workmate`，最后才允许 `desktop_agent` 兜底。

## 3. 通用 Agent 只做最后兜底

- 普通 MCP 模式不再直接暴露 `desktop_agent`。
- Agent 根据任务领域只发送相关工具 Schema，默认最多 16 个，而不是每轮 40+。
- 默认最多 4 轮，硬上限 8 轮。
- Agent API 单次超时默认 25 秒。
- 动作工具成功后直接返回结果，不再额外请求一次模型只为了说“完成”。
- 微信确认、定时授权、关闭窗口等安全规则仍然保留。

## 4. AI API 防“假死”

- 旧安装里保存的 120 秒默认超时自动迁移为 45 秒。
- SDK 自动重试默认 0。
- `auto` 模式只在“明确接口协议不兼容”时尝试另一套协议。
- 超时、断网、401/403、429、配额错误不再继续等第二遍协议。
- 性能计数会记录真实外部 API 请求次数，包括协议回退和兼容重试。

## 5. 写材料由 2 次 API 降到 1 次

V0.3 `write_material` 默认先用一次 API 写正文，再用一次 API 单独起标题。V0.4 改为一次请求同时返回“标题 + 正文”，本地再生成 DOCX；兼容接口不按 JSON 返回时，也不会为了标题再请求第二次模型。

## 6. 浏览器快速打开

- 普通“打开网页/搜索”不再等待 DevTools 冷启动最多 8 秒。
- 冷启动时直接用同一个专用 DevTools 浏览器配置启动并携带目标 URL，立即返回。
- 浏览器已经启动时直接创建标签页。
- 旧 `workmate`/开发者低层入口的 `browser_open` / `browser_search` 也统一重定向到快速路径。
- 真正需要 DOM 读取、点击、抓表格或联网调研时，才等待 DevTools 就绪。

## 7. 微信自动化加速与稳态处理

- 去掉大部分固定长 `sleep`，改成短等待 + 自适应 UIA 轮询。
- 优先 UIA 找聊天输入区并聚焦。
- UIA 失败才按“当前微信窗口相对位置”点击兜底，不写死屏幕坐标。
- 文件继续使用 Windows `CF_HDROP` 文件剪贴板 + `Ctrl+V`。
- 剪贴板被 Office/微信短暂占用时做短重试。
- 发送后在有限 UIA 范围和有限时间内验证，避免长时间全窗口扫描。
- 发送仍要求确认；未验证成功时明确提示用户查看微信，不伪报成功。

## 8. 启动和导入减负

普通模式不在 MCP Server 启动时主动加载整个 Office/网页调研工具栈。简单快车道任务不需要先加载 pandas/python-docx/pychrome 等重模块；只有任务确实进入 `workmate`/Office/调研时才加载相应模块。

## 9. 性能诊断

每个统一入口任务记录：

- 总耗时
- 命中的路由
- 外部 API 请求次数
- 工具调用数

可通过 `performance_report` 查看最近记录，数据同时保存在原来的 `actions.sqlite3`。

## 10. 升级体验

- 版本统一到 `0.4.0`。
- Inno Setup 保持相同 AppId，可覆盖升级。
- 安装新版时增加“关闭正在运行的旧 EXE”，避免旧托盘进程还在使用旧代码。
- 不删除 `%APPDATA%\XiaozhiDesktopAssistant`，旧 API/MCP/任务数据继续保留。
