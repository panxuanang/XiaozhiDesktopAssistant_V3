# V0.3.1 - 静默执行 / 精简汇报

- 新增电脑端 `speech_policy`：动作类任务默认只返回一句简短确认。
- “打开网页”不再隐式读取/总结网页；读取、总结必须由用户明确提出。
- 普通模式隐藏通用 `desktop_agent` MCP 工具，避免小智绕过本地确定性 `workmate` 路由；开发者兼容模式仍可使用。
- 通用 Agent 系统提示明确区分“动作 / 读取 / 汇报”，并在最终输出再次执行精简策略。
- 工具返回给 Agent 时按类别限长：读取类保留必要内容，动作类减少无意义大结果。
- 新增常用网站本地直开：百度、必应、Google、淘宝、京东、知乎、微博、小红书、B站；不经过 AI API。
- 去掉语音回复中的“执行方式/任务ID”等内部信息；完整执行信息仍保留在任务中心与日志中。
- 安全确认和错误原因不会被静默策略吞掉。
